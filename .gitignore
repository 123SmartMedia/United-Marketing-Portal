# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# Large binary asset library — hosted separately (see README). The junction
# public/assets points at it locally; do not commit the 3.7GB source library.
/UnitedMarketingDesk-Assets/
/public/assets

# generated catalog is committed for reproducible builds, but can be regenerated
# with `npm run catalog`. Uncomment to treat as build artifact:
# /src/content/catalog.json

# Alternate/earlier build extracted from unitedmarketingdesksite.zip — kept
# locally for reference, not part of this repo (root src/ build is canonical).
/site/

# Local agent/editor tooling config
/.claude/

